
name: Code Checks

on:
  pull_request:
    branches: main

jobs:
  Lint:
    runs-on: self-hosted
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2

      - name: Install Deps
        run: yarn

      - name: Code Linter
        uses: wearerequired/lint-action@v2
        with:
          eslint: true
